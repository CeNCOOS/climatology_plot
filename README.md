This code plots an anomaly vs the mean as color coded bars above and below the mean.  The  code assumes that the data are in a pandas dataframe with the index set as datetimes. 
An example json file for the code is supplied along with a driver to show how the code can be called.  

Dataset will not be supplied.
